{
  "name": "@zanthus/tokens",
  "scripts": {
    "build": "npm run build:tokens",
    "build:tokens": "style-dictionary build --config ./config/style-dictionary.config.json",
    "dev": "npm run build:tokens -- --watch",
    "clean": "rimraf build dist",
    "lint": "echo 'No linting for tokens'",
    "test": "echo 'No tests for tokens'"
  }
}