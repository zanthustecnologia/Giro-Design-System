/// Tamanhos padronizados para componentes do Design System
enum GiroSize { sm, md, lg, xl }

/// Posicionamento de elementos acessórios (como ícones ou badges)
enum GiroPosition { left, right, top, bottom, none }
