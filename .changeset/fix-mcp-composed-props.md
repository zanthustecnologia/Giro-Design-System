---
"@giro-ds/mcp": patch
---

Support local union and intersection prop types when generating component metadata, including Button, Select, TextArea, TextField and ToggleButton. Preserve documented examples and avoid marking variant-specific required props as globally required.
